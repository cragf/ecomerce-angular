import { FormatearPrecioPipe } from './formatear-precio-pipe';

describe('FormatearPrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearPrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
